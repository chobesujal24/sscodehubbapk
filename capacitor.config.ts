import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coderecreation.app',
  appName: ' LandioRecreation',
  webDir: 'dist'
};

export default config;
